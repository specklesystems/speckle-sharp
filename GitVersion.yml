mode: ContinuousDelivery
assembly-informational-format: "{Major}.{Minor}.{Patch}-{PreReleaseLabel}"
branches:
  main:
    regex: ^main$
    tag: rc
  dev:
    regex: ^dev$
    tag: beta
  pull-request:
    tag: wip
