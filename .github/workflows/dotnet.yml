# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  pull_request:
    branches: ["dui3/alpha", "dui3/ci/github-actions-test"]

jobs:
  build:
    runs-on: windows-latest
    strategy:
      matrix:
        include:
          - path: "DUI3-DX\\Connectors\\Revit\\Speckle.Connectors.Revit2023\\Speckle.Connectors.Revit2023.csproj"
            framework: net48
          - path: "DUI3-DX\\Connectors\\Revit\\Speckle.Connectors.ArcGIS3\\Speckle.Connectors.ArcGIS3.csproj"
            framework: net48

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 7.x.x

      - name: NuGet Cache
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/packages.lock.json') }}      

      - name: Format
        run: dotnet run --project Build/Build.csproj -- format

      - name: Restore
        run: dotnet run --project Build/Build.csproj -- restore ${{ matrix.path }}

      - name: Add msbuild to PATH
        uses: microsoft/setup-msbuild@v2

      - name: Build
        run: dotnet run --project Build/Build.csproj -- build ${{ matrix.path }}

      - name: Pack
        run: dotnet run --project Build/Build.csproj -- zip ${{ matrix.path }} ${{ matrix.framework }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          path: output/*.*
